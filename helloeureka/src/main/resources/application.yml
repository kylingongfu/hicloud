eureka:
  client:
    service-url:
      #设定eureka注册地址
      defaultZone: http://127.0.0.1:8761/eureka
    #不向注册中心进行注册
    register-with-eureka: false
spring:
  application:
    name: eureka
server:
  port: 8761